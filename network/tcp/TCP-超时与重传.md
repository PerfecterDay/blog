# TCP的超时与重传
{docsify-updated}

## 重传定时器
对每个连接，TCP管理4个不同的定时器：
1. **重传定时器**：发送方持有的定时器，使用于当希望收到另一端的确认 ack。如果没收到确认，将重传超时的数据。
2. **坚持(persist)定时器**：使窗口大小信息保持不断流动，即使另一端关闭了其接收窗口。
3. **保活(keepalive)定时器**：可检测到一个空闲连接的另一端何时崩溃或重启。
4. **2MSL定时器**：测量一个连接处于 `TIME_WAIT` 状态的时间。

